flask>=3.0
pymupdf>=1.24
reportlab>=4.0
anthropic>=0.19
openai>=1.1
# faiss-cpu 1.8.x wheels were built against NumPy 1 and fail to import under
# NumPy 2 ("numpy.core.multiarray failed to import"). 1.9+ supports both.
faiss-cpu>=1.9,<2
numpy>=1.26
sentence-transformers>=2.7
python-dotenv>=1.0
werkzeug>=3.0
requests>=2.31

# Optional: `python embedding_models_cli.py list --source mteb` computes the
# STS ranking from the full MTEB results repository (large first download).
# mteb>=2.0
